<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- The simplest way to build a service with service-sdk-starter-core
      is to use it as a parent in your project’s POM file, and alternative If you
      don’t want to use service-sdk-starter-core as your project’s parent, you
      can declare it as a dependency instead, see pom-as-dependency.xml -->
  <parent>
    <groupId>com.backbase.buildingblocks</groupId>
    <version>11.2.1</version>
    <artifactId>service-sdk-starter-core</artifactId>
    <relativePath/>
  </parent>

  <groupId>com.backbase</groupId>
  <artifactId>services</artifactId>
  <packaging>pom</packaging>
  <version>0.0.1-SNAPSHOT</version>

  <modules>
    <module>product</module>
    <module>review</module>
    <module>store</module>
  </modules>

  <properties>
    <java.version>11</java.version>
    <org.lombok.version>1.18.12</org.lombok.version>
    <properties-maven-plugin.version>1.0.0</properties-maven-plugin.version>
    <testcontainers.version>1.14.2</testcontainers.version>
    <boat-maven-plugin.version>0.10.0</boat-maven-plugin.version>
    <docker.repo.project>experimental/golden-sample</docker.repo.project>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Start - POJO creation made easy -->
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>${org.lombok.version}</version>
        <optional>true</optional>
      </dependency>
      <!-- End - POJO creation made easy -->
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers</artifactId>
        <version>${testcontainers.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>mysql</artifactId>
        <version>${testcontainers.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>mssqlserver</artifactId>
        <version>${testcontainers.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>mariadb</artifactId>
        <version>${testcontainers.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>io.swagger</groupId>
        <artifactId>swagger-annotations</artifactId>
        <version>1.6.0</version>
      </dependency>
      <dependency>
        <groupId>org.openapitools</groupId>
        <artifactId>jackson-databind-nullable</artifactId>
        <version>0.2.1</version>
      </dependency>
      <!-- https://mvnrepository.com/artifact/mysql/mysql-connector-java -->
      <dependency>
        <groupId>mysql</groupId>
        <artifactId>mysql-connector-java</artifactId>
        <version>5.1.47</version>
      </dependency>
      <!-- https://mvnrepository.com/artifact/com.microsoft.sqlserver/mssql-jdbc -->
      <dependency>
        <groupId>com.microsoft.sqlserver</groupId>
        <artifactId>mssql-jdbc</artifactId>
        <version>7.4.1.jre8</version>
      </dependency>
      <dependency>
        <groupId>org.mariadb.jdbc</groupId>
        <artifactId>mariadb-java-client</artifactId>
        <version>2.6.2</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>com.backbase.oss</groupId>
          <artifactId>boat-maven-plugin</artifactId>
          <version>${boat-maven-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>versions-maven-plugin</artifactId>
          <version>2.7</version>
        </plugin>
        <plugin>
          <artifactId>maven-assembly-plugin</artifactId>
          <version>3.3.0</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <profiles>
    <profile>
      <id>generateSQL</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-maven-plugin</artifactId>
            <version>3.10.2</version>
            <executions>
              <execution>
                <id>mysql</id>
                <configuration>
                  <changeLogFile>${project.basedir}/src/main/resources/db/db.changelog-master.yaml</changeLogFile>
                  <url>offline:mysql?outputLiquibaseSql=none&amp;changeLogFile=${project.basedir}/src/main/resources/db/sql/mysql/databasechangelog.csv</url>
                  <username>root</username>
                  <password>backbase</password>
                  <migrationSqlOutputFile>${project.basedir}/src/main/resources/db/sql/mysql/create.sql</migrationSqlOutputFile>
                </configuration>
                <goals>
                  <goal>updateSQL</goal>
                </goals>
                <phase>process-resources</phase>
              </execution>
              <execution>
                <id>mssql</id>
                <configuration>
                  <changeLogFile>${project.basedir}/src/main/resources/db/db.changelog-master.yaml</changeLogFile>
                  <url>offline:mssql?outputLiquibaseSql=none&amp;changeLogFile=${project.basedir}/src/main/resources/db/sql/mssql/databasechangelog.csv</url>
                  <username>root</username>
                  <password>backbase</password>
                  <migrationSqlOutputFile>${project.basedir}/src/main/resources/db/sql/mssql/create.sql</migrationSqlOutputFile>
                </configuration>
                <goals>
                  <goal>updateSQL</goal>
                </goals>
                <phase>process-resources</phase>
              </execution>
              <execution>
                <id>oracle</id>
                <configuration>
                  <changeLogFile>${project.basedir}/src/main/resources/db/db.changelog-master.yaml</changeLogFile>
                  <url>offline:oracle?outputLiquibaseSql=none&amp;changeLogFile=${project.basedir}/src/main/resources/db/sql/oracle/databasechangelog.csv</url>
                  <username>root</username>
                  <password>backbase</password>
                  <migrationSqlOutputFile>${project.basedir}/src/main/resources/db/sql/oracle/create.sql</migrationSqlOutputFile>
                </configuration>
                <goals>
                  <goal>updateSQL</goal>
                </goals>
                <phase>process-resources</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>generateChangeLog</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-maven-plugin</artifactId>
            <version>3.10.2</version>
            <configuration>
              <changeLogFile>${basedir}/src/main/resources/db/db.changelog-master.yaml</changeLogFile>
              <propertyFile>${basedir}/src/main/resources/liquibase.properties</propertyFile>
              <outputChangeLogFile>${basedir}/src/main/resources/db/changelog/000-create.yaml</outputChangeLogFile>
            </configuration>
            <executions>
              <execution>
                <goals>
                  <goal>generateChangeLog</goal>
                </goals>
                <phase>generate-resources</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <repositories>
    <repository>
      <id>backbase</id>
      <url>https://repo.backbase.com/repo</url>
      <releases>
        <updatePolicy>always</updatePolicy>
      </releases>
    </repository>
  </repositories>
  <pluginRepositories>
    <pluginRepository>
      <id>backbase</id>
      <url>https://repo.backbase.com/repo</url>
    </pluginRepository>
  </pluginRepositories>

</project>
